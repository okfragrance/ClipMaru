# ClipMaru 実装ハンドオフドキュメント

## 概要

Windows 11向けクリップボード管理アプリ。既存ツール「Clibor」の代替として、個人利用目的で開発。
Cliborの不満点(並べ替えにCSV書き出しが必要、書式の扱いが面倒、カテゴリ選択のクセ)を解消しつつ、
19カテゴリ×最大40件規模の大量登録運用に耐える設計とする。

- 開発者: くにこ (K-Studio3070 / AnnyStation)
- プラットフォーム: Windows 11
- 技術スタック: Tauri v2 + React
- ストレージ: SQLite必須(localStorage禁止。NAGI Workspaceでの保存失敗事例あり)
- HOUSE_RULES.md / BRAND_TOKENS.md をプロジェクトのKnowledge/CLAUDE.mdに登録して適用すること

参照プロトタイプ: `clipboard_prototype_v3.html`(単一HTML、本ドキュメントの仕様はこのプロトタイプで確定した挙動を正としている)

---

## 画面構成

常駐アプリ。タスクバー常駐、クリックでポップアップ表示。ウィンドウは細長い形状(幅300px程度)を基本とし、
「表示しながら別ウィンドウで作業する」使い方を想定する。

タブは2つ: **履歴** / **定型文**

---

## 1. 履歴タブ

### 機能
- OSのクリップボード履歴を自動記録し、一覧表示(時刻は表示不要)
- 大量に保持できること(件数上限は要検討だが、少なくとも数百件は保持できる設計とする)
- 検索機能は不要(ユーザーが不要と判断済み)
- 1行1件のコンパクトな一覧表示(タイトルのみ、複数行表示は不要)
- クリックでコピー(既定ではコピー時の書式をそのまま保持: リッチはリッチのまま)
- リッチテキスト項目にのみ、行の右側に「リッチ/プレーン」を示す小さなトグルボタンを表示
  - 初期状態は「リッチ」ラベル
  - 押すと表示が「プレーン」に切り替わり、以後そのアイテムはプレーンテキストとしてコピーされる
  - 再度押すとリッチに戻せる(トグル)
  - プレーン項目にはボタン自体を表示しない(視覚的にすっきりさせる)

---

## 2. 定型文タブ

### カテゴリ管理
- カテゴリ数: 19程度を基準に、可変(ユーザーが追加/削除できる)
- カテゴリ切り替えのメインUI: **ダイヤル**
  - マウスホイールを当てると1つずつ切り替わる(Cliborの「マウスコロコロ」相当の挙動)
  - ダイヤルは常に画面上部に固定表示(sticky)、スクロールしても隠れない
  - ダイヤル中央のカテゴリ名、または▼ボタンをクリックすると「カテゴリ一覧ポップアップ」が開く
  - ▲ボタンは1つ前のカテゴリに戻る(一覧は開かない)
- カテゴリ一覧ポップアップ
  - 全カテゴリを一覧表示、クリックで直接ジャンプ
  - 現在選択中のカテゴリをハイライト
  - リスト最下部に「新しいカテゴリ名」入力欄+「追加」ボタン(その場で新規カテゴリを作成し、作成後は自動的にそのカテゴリに移動)
  - 各カテゴリ項目に削除ボタン(🗑)を表示
    - 押すと確認ダイアログ(「◯件の定型文も削除されます」等、件数を明示)
    - カテゴリが最後の1つの場合は削除不可(トースト通知)

### 定型文の登録・表示
- データ構造: 各定型文は **見出し(label)** と **中身(content)** を分けて持つ
  - 見出しは一覧に表示される短いテキスト
  - 中身は実際にコピー・貼り付けされる長文
  - 見出しは既定で「中身の先頭行(14文字程度)」から自動生成する
  - ユーザーが編集パネルで見出しを手動入力した場合はそれを優先。見出し欄を空にして保存すると自動生成に戻る
- 一覧は1行1件のコンパクト表示(カード形式ではない)。40件規模でもスクロールで見やすい密度とする
- 行をクリックすると「中身」をクリップボードにコピー(見出しではなく中身がコピーされる)
- 行にカーソルを合わせると✎(編集)アイコンが表示され、押すと編集パネルが開く
  - 編集パネル: 見出し入力欄(空欄可) + 中身のテキストエリア + キャンセル/保存ボタン
- ドラッグ&ドロップで自由に並べ替え可能。ドラッグハンドル(⠿)は行の右端に配置(履歴行と視覚的に揃える)
- 定型文の削除: **常時表示のアイコンは置かない**(ドラッグ操作時の誤爆を避けるため)。右クリックメニューから行う(後述)

### 区切り線(グループ分け)
- 定型文リストの中に「区切り線」を自由に挿入できる(Cliborにあった`------`区切りの再現+α)
- 区切り線には短いラベルを持たせられる
  - 既定は自動採番: ラベル未設定の区切り線は上から「グループ1」「グループ2」…と自動的に連番が振られる
  - ラベルをダブルクリックして手動編集すると、その区切り線は自動採番から除外され、固定の名前になる。残りの自動採番の区切り線は詰めて番号が振り直される
- 区切り線ごとに「#」ボタン(グループ番号表示のON/OFF)
  - ONにすると、その区切り線から次の区切り線までの定型文に「1. 」「2. 」…という**見た目だけの連番**が付く
  - 見た目のみの機能であり、見出し・中身のテキストデータには一切反映されない(コピーされる内容にも編集パネルにも番号は含まれない)
  - グループごとに個別にON/OFF可能
- 区切り線の削除: 区切り線自体を削除しても、配下の定型文は削除されない

### 追加ボタンの配置
- 「＋ 定型文」「＋ 区切り線」ボタンは、カテゴリダイヤルのすぐ下、常に画面上部に固定表示(sticky)
  - 40件スクロールしても、末尾までスクロールしなくても追加操作ができるようにするため

### 右クリックコンテキストメニュー
リスト上で右クリックすると、カーソル位置にメニューを表示。右クリックした行の直前にアイテムを挿入する。

メニュー項目:
1. **ここに定型文を追加** — 編集パネルを開き、保存するとその位置に新規挿入
2. **ここに区切り線を追加** — その位置に区切り線を直接挿入(グループは自動採番)
3. **選択して移動...**(右クリックした対象が定型文の場合のみ表示) — 下記「選択モード」参照
4. **削除**(赤字) — 右クリックした対象(定型文または区切り線)を削除。確認ダイアログあり
   - 区切り線の場合は「中の定型文は削除されません」と明示

何もない余白で右クリックした場合は、リスト末尾への追加のみ選択可能(移動・削除は出ない)。

### 選択モード(複数選択してカテゴリ移動 / まとめて削除)
- 発生頻度は低い操作のため、常時表示のUIは持たず、右クリックメニュー経由でのみ開始する
- 「選択して移動...」を押すと選択モードに入る(右クリックした項目は選択済み状態でスタート)
- 選択モード中:
  - 追加ボタン(＋定型文/＋区切り線)は非表示になり、代わりに選択バーが表示される
    - 「◯件選択中」+「移動」ボタン+「削除」ボタン+「キャンセル」ボタン
  - 各定型文行はチェックボックス風の表示に変わり、クリックでON/OFF切り替え、複数選択可能
  - ドラッグ&ドロップ、区切り線の編集・番号トグルは無効化する(操作の混線防止)
- 「移動」を押すとカテゴリ一覧ポップアップが「移動先を選択」モードで開く
  - 既存カテゴリを選ぶとまとめて移動
  - その場で新規カテゴリを作成し、そこに直接移動することも可能
- 「削除」を押すと選択中の項目をまとめて削除(確認ダイアログあり)
- 「キャンセル」でいつでも選択モードを抜けられる

---

## 3. 常時表示 / ウィンドウ挙動

- タイトルバーに「常に表示」ON/OFFトグルスイッチを設置
  - ONにするとウィンドウが常に最前面に表示される(Tauriのalways-on-top機能を使用)
- ウィンドウ形状は細長め(幅300px程度)。「表示しながら他のウィンドウで編集」という使い方を想定
- **システムトレイからの起動時、直前のウィンドウ位置を記憶して復元すること**
  - `tauri-plugin-window-state` 等を利用し、終了時の位置・サイズを保存、次回起動時に復元する実装とする

---

## 4. バックアップ機能

- タイトルバーに⚙(設定)アイコンを設置し、バックアップパネルを開く
- **バックアップを保存**: カテゴリ・定型文データ一式をJSONファイルとして書き出し(ファイル名に日付を含める)
- **バックアップから復元**: JSONファイルを選択して読み込み、現在のデータを丸ごと上書き復元
  - 実装時はSQLiteが正のデータストアとなるため、エクスポート/インポートはSQLiteの内容をJSON形式でシリアライズ/デシリアライズする形で実装する
  - 復元前に「現在のデータは上書きされます」等の確認を入れることを推奨

---

---

## 5. 配色テーマ

- デフォルト配色(クリーム×オレンジのkawaii系、現行プロトタイプの配色)は確定。これをベースにする
- 壁紙やダークモードとの兼ね合いで浮いて見えることがあるため、**複数の配色パターンを切り替えられる**ようにする
  - 少なくともダークモード相当のパターンを1つは用意
  - 他のパターン数・具体的な配色は実装時にBRAND_TOKENS.mdとあわせて検討(B7アコシャントカラーの件も合わせて詰める)
- 切り替えはタイトルバーの⚙(設定)パネルなど、既存のバックアップ設定と同じ場所にまとめてよい
- CSS変数(`--cream`, `--ink`, `--accent`等)は現行プロトタイプの設計を踏襲し、テーマ切り替え時はこれらの変数セットを差し替える方式で実装する

---

## データモデル(参考)

```
Category {
  id
  name
  order
}

PhraseListItem {  // カテゴリごとの1件
  id
  category_id
  order
  type: 'phrase' | 'divider'

  // type = 'phrase' の場合
  label        // 見出し(表示用)
  content      // 実際にコピーされる中身
  label_is_auto // 見出しが自動生成かどうか

  // type = 'divider' の場合
  label
  label_is_auto // 自動採番中かどうか(手動編集されるとfalseに)
  numbered      // このグループの見た目連番表示ON/OFF
}

HistoryItem {
  id
  content
  format: 'plain' | 'rich'
  created_at
  // 表示上は時刻を出さないが、内部的には保持しておいて良い(将来的な整理機能等のため)
}
```

---

## Claude Code実装時の主な技術検討事項

1. **クリップボード監視**: WindowsのクリップボードをTauri側で監視し、履歴に自動追加する実装(`arboard` crateやWindows API経由でのポーリング/イベント監視を検討)
2. **リッチ/プレーンの実体**: 履歴保存時にリッチテキスト(HTML/RTF)とプレーンテキストの両方を保持し、コピー時にどちらを書き込むか選択する形にする
3. **常時最前面**: TauriのWindow APIで`set_always_on_top`を切り替え
4. **ウィンドウ位置記憶**: `tauri-plugin-window-state`の導入
5. **SQLite**: カテゴリ・定型文・履歴すべてSQLiteで管理。localStorageは使用しない
6. **システムトレイ**: クリックでウィンドウ表示/非表示をトグル

---

## 対象外(今回のスコープ外)

- 番号/アルファベットキーによるホットキー直接呼び出し(ユーザー判断により不要と確定)
- 履歴の検索機能(ユーザー判断により不要と確定)

---

## 参照

- プロトタイプファイル: `clipboard_prototype_v3.html`
- 適用するプロジェクト共通ルール: `HOUSE_RULES.md`(R1-R9)、`BRAND_TOKENS.md`(B1-B7)
