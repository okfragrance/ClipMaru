# BRAND_TOKENS.md

> 🔗 このファイルは正本のコピーです。正本: [`../../../K-Studio3070_Template-Claude/BRAND_TOKENS.master.md`](../../../K-Studio3070_Template-Claude/BRAND_TOKENS.master.md)
> 変更する場合は、まず正本を更新してからこちらに反映してください。ClipMaru固有の承認済み逸脱（B5-3）はこのファイルに直接追記してよい。
> 最終同期日: 2026-09-18（正本 v0.3 に同期）

---

### K-Studio3070（AnnyStation内レーベル）ビジュアル基準 v0.3

> HOUSE_RULES.md（コード規約）に対応するビジュアル版。
> 「違反を構造的に書けないようにする」思想を踏襲し、Tauri v2/React・Flutter両テンプレートの
> テーマ定義（CSS variables / ThemeData）にそのまま焼き込める粒度で書く。

---

## 0. ブランド階層

```
AnnyStation（会社・屋号）
└─ K-Studio3070（プロダクトレーベル）
    ├─ Macaron Board（基準作品）
    ├─ PatisserieClicker（基準作品）
    ├─ amitto（準拠・軽量版）
    └─ POMO Quest（逸脱許容ジャンル：レトロゲーム演出）
```

NAGI Workspaceはブランド対象外（戦略的に切り捨て。既存トークンとの整合は取らない）。

---

## B1. パレット（Macaron Board / PatisserieClickerの実測値ベース）

### コアパレット
| 役割 | HEX | 由来 |
|---|---|---|
| ベース背景（クリーム） | `#FAEBD4` | PatisserieClicker背景 |
| ベース背景（薄クリーム/白寄り） | `#FFF9F5` | amitto背景（許容差分） |
| アウトライン・テキスト濃色（焦げ茶） | `#3A2A26` | PatisserieClickerボタン輪郭・実測最頻値 |
| アウトライン濃色（サブ） | `#55423A` | Macaron Boardアイコン輪郭 |
| 焦げ茶サブトーン（皮・木目） | `#8F553C` | PatisserieClicker装飾 |

### アクセント5色（パティスリーパレット）
| 名称 | HEX | 由来・用途 |
|---|---|---|
| いちご（ローズ） | `#E0567A` | PatisserieClickerきらめきカード／プライマリCTA |
| ピスタチオ（ミント） | `#E3EFCB` | Macaron Boardカードベース／サブアクション |
| レモン（イエロー） | `#FEE294` | PatisserieClicker生地カード／通貨チップ |
| ラベンダー（薄紫） | `#EAD9F8` | PatisserieClicker名声カード／情報系 |
| ベビーピンク | `#FEBBC8` / `#ED93B1` | amitto・小物装飾 |
| アンバー（琥珀） | `#F2A623` | PatisserieClickerオーブン熱ゲージ中域／フィーバー系グラデーション。レモンといちごの間を埋める暖色 |

> 上記は実測サンプリング値で、OKLCH 明度は `0.64`（いちご）〜`0.93`（ピスタチオ）とばらつく（2026-09-18 実測）。
> ビビッド側はプロダクトの実測値なのでこのまま据え置き、明度・彩度を揃えた版は下の「くすみ tier」として別系統で持つ。

### CTA の文字色（v0.3 新設・実測）

いちご `#E0567A` に焦げ茶文字を乗せたコントラスト比は **3.76 : 1**、白文字は **3.63 : 1**。どちらも WCAG AA（4.5）を小さい文字では満たさない。

| ラベルサイズ | 地 | 文字 | 比 |
|---|---|---|---|
| **19px 太字以上**（AA 大文字基準 3.0） | いちご `#E0567A` | 焦げ茶 `#3A2A26` | 3.76 ✅ |
| 19px 未満 | **深いちご `#BD4264`** | 白 `#FFFFFF` | 5.09 ✅ |
| 任意 | レモン `#FEE294` / アンバー `#F2A623` | 焦げ茶 | 10.73 / 6.66 ✅ |

深いちごは「小さいラベルの CTA 専用」。カード地やチップには使わない。

### くすみ tier（v0.3 新設）

現行6色の **OKLCH 色相を保ったまま**、明度 L と彩度 C を3段に揃えて再サンプリングしたもの。ベビーピンク（h=6°）はいちご（h=7°）と色相がほぼ同じでくすませると同色になるため、tier ではいちごに統合する。

| 色相 | soft `L0.90 C0.045`<br>面・カード地 | dusty `L0.80 C0.075`<br>チップ・サブボタン | deep `L0.66 C0.11`<br>アイコン・ゲージ・線 |
|---|---|---|---|
| いちご | `#FAD2D9` (9.93) | `#E9AAB5` (7.07) | `#CB7486` (4.15) |
| アンバー | `#F0DABE` (10.06) | `#DBB788` (7.24) | `#BA873D` (4.30) |
| レモン | `#E9DDBD` (10.10) | `#D0BC86` (7.29) | `#AC8F38` (4.38) |
| ピスタチオ | `#D8E3C3` (10.20) | `#B5C690` (7.44) | `#869D4C` (4.51) |
| ラベンダー | `#E7D6F5` (9.96) | `#CDB1E2` (7.14) | `#A77FC5` (4.21) |

括弧内は焦げ茶 `#3A2A26` の文字を乗せたときのコントラスト比。

**くすみ用ニュートラル**
| 役割 | HEX | 用途 |
|---|---|---|
| greige-bg | `#F3E9DF` | くすみパターンのベース背景（クリーム族。B5-2 条件①を満たす） |
| greige-card | `#FBF6F0` | カード地 |
| greige-line | `#CEC1B8` | 柔らかい区切り線（ソフト影パターン用） |
| mocha | `#503E35` | くすみ世界のアウトライン・影・本文（焦げ茶の1トーン明るい版。greige-bg 上 8.42） |
| mocha-2 | `#7B6358` | 補助テキスト（greige-card 上 5.19） |

**使用ルール**
1. **soft と dusty は文字の地に使ってよい**（焦げ茶文字で 7 : 1 以上）。**deep は文字の地に使わない**（4.2 前後で AA 境界上）。アイコン・ゲージ・線・大見出しの色に限る。
2. 同一画面でビビッド色と dusty を混ぜるのは **P1（選択＝ビビッド／非選択＝dusty）の用途に限る**。それ以外はどちらか一方に寄せる。
3. P2 / P3 のアウトラインは mocha `#503E35`。焦げ茶 `#3A2A26` はくすみ地に対して黒く沈むので使わない。

**使い方の3パターン（参照実装は `BRAND_TOKENS` 改訂案アーティファクト 2026-09-18）**
| パターン | 想定 | 内容 |
|---|---|---|
| **P1 状態くすみ** | 既存のパティスリー系アプリ | 選択中だけビビッド＋影 `sm`、非選択は dusty で影なし・枠 `1.5px`。DeskBento が枠の太さで作っていた選択シグナルを彩度でも作る |
| **P2 くすみ全面 × ハードシャドウ** | 「喫茶パティスリー」の温度感 | B2 署名を全部維持（枠 2px・ハード影・角丸）。アウトライン＝mocha、CTA＝dusty いちご、強調は deep をゲージ・線で。彩度ではなく影で主役を立てる |
| **P3 くすみ全面 × ソフト影** | B5-2 ライフスタイル枠 | greige 地、枠 `1px` greige-line、影 `0 2px 8px rgba(58,42,38,.12)`。みどり手帖の実装を tier に載せ替えたもの |

---

## B2. 署名パーツ3点（これが崩れるとブランドが崩れる）

### ① アウトライン + ハードドロップシャドウ
- ボーダー: `2px solid #3A2A26`（太め、焦げ茶固定）
  - 「面」（カード・パネル・ヘッダー）は `2px` 固定。「操作子」（チップ・小ボタン）は DPR ≥ 2 の画面に限り `1.5px` を許容（等倍では `1px` に丸められて非選択と区別できなくなるため。DeskBento の B5-3 記録を参照）
- シャドウ: ぼかし0（ハードシャドウ）、色はボーダーと同色。**オフセットは役割別の3段スケール**（v0.3）

  | トークン | オフセット | 適用先 |
  |---|---|---|
  | `shadow-none` | `0` | 影付きの面の内側にある非操作要素（チップ・バッジ・内側カード）、リスト行、非選択チップ |
  | `shadow-sm` | `2px` | 通貨チップ、小アイコンボタン（高さ 40px 未満） |
  | `shadow-md` | `3px` | ボタン、カード、パネル（**既定値**。v0.2 の `4px` から 1px 下げ） |
  | `shadow-lg` | `4px` | モーダル、浮遊パネル、ドラッグ中の要素のみ |

  > 根拠: 影 4px + 枠 2px は同色なので右下に 6px の帯ができる。カード（高さ 220px）では 3% で心地よいが、チップ（高さ 33px）では 18% を帯が占めて重くなる。原因は色ではなく比率なので、色は薄めず、オフセットをサイズで使い分ける。

- **ネスト規則**: 影が付くのは (a) 操作できるもの（ボタン・アイコンボタン）、(b) 最外殻の面、(c) 浮遊物、の3種だけ。**影付きの面の内側にある非操作要素は枠線のみ**（影の中に影を入れない）。深さは影の段数ではなくオフセットの大きさで表す。テンプレートでは `.k3070-outline-shadow .k3070-outline-shadow { 影なし }` で構造的に縛っている。
- 適用対象: プライマリボタン、カード、主要アイコン枠、通貨チップ

### ② 角丸スケール
- カード/パネル: `16px`
- ボタン: `12px`
- チップ/バッジ: `999px`（完全ピル）
- 小アイコンボタン: `10px`

### ③ 通貨チップ構造（Macaron Board / POMO Quest共通の署名パターン）
```
[アイコン(コイン等)] + [数値] を角丸ピルに内包、画面右上固定配置
例: 🟡 146 / 🪙 300
```
新規プロジェクトで「所持数・スコア表示」が要る場合は、この構造をデフォルトにする。通貨チップの影は `shadow-sm`。

### ④ 状態トークン（v0.3 新設・署名パーツの手触り）

ハードシャドウは「押し込める」ことで初めて装飾ではなく操作の手触りになる。ボタン・アイコンボタンには以下を必ず実装する（テンプレートの `.k3070-button` / `K3070Button` に焼き込み済み）。

| 状態 | 見た目 |
|---|---|
| hover | `translate(-1px, -1px)`、影オフセット +1px（1px 浮く） |
| active | `translate(o, o)`、影 0（影の分だけ沈む）。トランジション 80ms、`prefers-reduced-motion` では無効 |
| focus-visible | いちご `#E0567A` の 3px リング、offset 3px |
| disabled | 影なし、地 `#E7D6BE`、枠 `#B7A594`、文字 `#8A7A6E`、`cursor: not-allowed` |
| CTA 文字 | ラベル 19px 太字以上 → いちご地＋焦げ茶文字。19px 未満 → 深いちご `#BD4264`＋白文字（B1「CTA の文字色」参照） |

---

## B3. タイポグラフィ

- **メイン書体**: 丸ゴシック系1書体に統一（候補: `Zen Maru Gothic` / `M PLUS Rounded 1c` / `Kosugi Maru`）
  - 🔲 **確定タスク**: Macaron BoardとPatisserieClickerで丸みの強さに微差あり。どちらかに正式統一する。
- **太さ**: 見出し・ボタンラベルは太字（Bold〜ExtraBold）、本文は Regular〜Medium
- **数字表示**（カウンター・スコア等）: 装飾より視認性優先。等幅寄りの太字でOK（POMO Questのようにジャンル演出でドット絵フォントに逸脱するのは許容）

---

## B4. アイコン・イラストのトーン

- 線画+パステル塗り、輪郭線は焦げ茶固定（B1準拠）
- キャラクター（猫シェフ等）はふっくら丸みのあるプロポーション
- 過度な写実・グラデーション多用は避ける（フラット〜セミフラット）

---

## B5. 逸脱ルール（ジャンル演出との両立）

ゲーム性の強い作品（POMO Quest、今後のエルデ辺境伯領など）では、世界観演出のための逸脱を許可する。ただし以下は死守：

1. **アウトライン色は焦げ茶系（`#3A2A26`前後）から大きく外さない**
   - POMO Questの黒シャドウは焦げ茶寄りに寄せることを推奨
2. **彩度レンジを保つ**（ブランドパステルの彩度域を超えて原色に振り切らない）
   - POMO Questの紫背景は現状やや彩度過多。ラベンダー`#EAD9F8`方向にワントーン落とすと接続感が出る
3. **通貨チップ・角丸ピルなど「構造パターン」は維持**（見た目のテクスチャは変えてよいが配置構造は変えない）
4. 逆に、フォント・アイコンの描き込み密度・直角/曲線の使い分けは、ジャンルに応じて自由に変えてよい

エルデ辺境伯領がピクセル/レトロ寄りの見た目になる場合、POMO Questと同じ「逸脱ルール」の適用例として扱う。

### B5-2. ライフスタイル/ジャーナル系カテゴリ（新設）

ゲーム演出とは別に、「静かで上質」な情緒を狙うライフスタイル・記録・手帳系アプリ用の逸脱枠。パティスリー系の「甘くてポップ」なテンションとは狙う情緒自体が異なるため、B2の署名パーツ（焦げ茶太アウトライン＋ハードシャドウ）は要求しない。

**維持すべき2点のみ：**
1. **クリームベースファミリーを保つ**（B1のベース背景色域から大きく外れない。暖色寄りのニュートラル背景であればOK）
2. **メイン書体はZen Maru Gothicで統一**（B3のメイン候補書体と一致させる）

**自由にしてよい範囲：**
- アウトライン太さ・シャドウ表現（柔らかいブラー影も許容。ハードシャドウを強制しない）
- アクセント色相（B1の4色に限定しない。落ち着いた暖色・自然色系であれば範囲内とみなす）
- サブ書体の追加（例：英字ラベルにセリフ斜体を重ねるなど、世界観に応じた装飾フォントの併用）
- 独自の署名パーツ形状の新設（例：葉っぱ型アイコンのような、そのアプリ固有のモチーフ）

**実例：みどり手帖（植物記録アプリ）**
- ベース: 羊皮紙クリーム `#F0E7D0`〜`#DFD1AF` / カード `#FCF8EE`
- 書体: Zen Maru Gothic（見出し）＋ Newsreader italic（英字ラベル装飾）
- アクセント: テラコッタ `#D07C57` → **v0.3 で deep いちご `#CB7486` を推奨**（B1「くすみ tier」）。色相が B1 のいちごと繋がり、パティスリー系との接続感が出る。テラコッタのまま残す場合は B5-3 に記録する
- くすみ tier での再構成例は B1「P3 くすみ全面 × ソフト影」
- シャドウ: 柔らかいブラー影で統一（B2のハードシャドウは不採用）
- 署名パーツ: `border-radius: 0 100% 16% 100%` の葉っぱ型アイコン

このカテゴリに該当する新規プロジェクトが出てきた場合、この2作品（今後増えれば追記）を参照実装として使う。

### B5-3. 逸脱記録（プロジェクト別・承認済み）

ジャンル演出以外の理由で基準から外す場合は、口頭合意で終わらせず必ずここに残す。
`/audit` はこの表を見て「未記録の逸脱」だけを違反として扱う。各プロジェクトは、この記録フォーマットに沿って
自プロジェクト固有の承認済み逸脱を、自分の `BRAND_TOKENS.md`（正本のコピー）のこのセクションに追記する。

記録フォーマット:
| 項目 | 内容 |
|---|---|
| 対象 | 逸脱するコンポーネント／要素 |
| 基準 | 該当するB1〜B4の基準項目 |
| 逸脱内容 | 具体的な差分 |
| 理由 | なぜ逸脱するか |
| 死守している条件 | 逸脱してもよい範囲の境界線 |
| 実装 | 実装方法・トークン化の方針 |
| 記録日 | YYYY-MM-DD（audit tagがあれば併記） |

#### 参照実装例：DeskBento — ボーダー太さ（B2① の例外）

| 項目 | 内容 |
|---|---|
| 対象 | プリセットピル（`.preset-pill`）／アクションピル（`.action-pill`）／ヘッダーボタン（`.header-btn`） |
| 基準 | B2①「ボーダー: `2px solid #3A2A26`」 |
| 逸脱内容 | 通常時 `1.5px` ／ 選択中のみ `2px` ／ コンパクトモード時 `1px`（選択中は `2px` 据え置き） |
| 理由 | DeskBentoは1画面に小さなピルが10個以上並ぶ密度の高いツール型UIで、全ピルを2pxにすると線が支配的になり「お弁当」の軽さが出ない。あわせて、太さの差を選択状態のシグナルとして使う（塗りの濃さだけに頼らない）。 |
| 死守している条件 | ①アウトライン色は `#3A2A26` 固定のまま ②カード・ヘッダー・プリセットバー等の「面」は基準どおり `2px` を維持 ③ハードドロップシャドウ・角丸ピル・角丸スケールなど構造パターンは一切変更なし |
| 実装 | `style.css` の `--border-width-default` / `--border-width-active` / `--border-width-compact` の3トークンのみで管理（コンポーネント側に実値を書かない） |
| 記録日 | 2026-08-12（audit-20260812 第4弾） |

> 補足: コンパクト時の選択中を `1.5px` ではなく `2px` にしているのは、等倍ディスプレイでは
> ブラウザが `1.5px` を `1px` に丸めてしまい、非選択（`1px`）と見分けがつかなくなるため。

他プロジェクトで承認済み逸脱が発生した場合は、同じフォーマットでこのセクションに追記していく（プロジェクト固有のデータなので、正本には代表的な参照実装例のみを残し、全件はコピー先に置く）。

---

## B6. 実装メモ（テンプレート化のヒント）

- Tauri v2/React: `theme.css` に上記HEXをCSS variables化（`--k3070-outline`, `--k3070-accent-strawberry` 等）。影は `--k3070-shadow-sm / -md / -lg` と `.k3070-shadow-sm / -lg / -none` 修飾クラス、状態は `.k3070-button` / `.k3070-icon-button` に内包。くすみ tier は `--k3070-dusty-<tier>-<hue>` / `--k3070-greige-*` / `--k3070-mocha`
- Flutter: `ThemeData` の `ColorScheme` + カスタム `BoxDecoration` プリセット（アウトライン+シャドウ）をウィジェット化して使い回す。影は `K3070Shadow.none / sm / md / lg`、状態は `K3070Button`、くすみ tier は `K3070Dusty`
- どちらも「B2の署名パーツ」をコンポーネント化しておけば、新規プロジェクトで自然にブランド内に収まる（HOUSE_RULES R1〜R9と同じ「構造で縛る」発想）

---

## B7. 未確定・要判断リスト

- [ ] メイン書体の正式決定（Zen Maru Gothic / M PLUS Rounded 1c / Kosugi Maru）
- [ ] amittoのカード：細ボーダーのまま行くか、B2のアウトライン+シャドウを主要ボタンだけに導入するか
- [ ] POMO Quest（および今後のエルデ辺境伯領）の彩度・シャドウ色の実際の調整
- [ ] NAGI Workspaceの取り扱い最終確認（対象外のまま／最小介入すら不要か）
- [ ] みどり手帖：アクセント色を deep いちご `#CB7486` に載せ替えるか（v0.3 推奨）、テラコッタ `#D07C57` を B5-3 に記録して残すか
- [ ] v0.3（影スケール・状態・くすみ tier）の各プロダクトへの配布。テンプレート2種は 2026-09-18 に反映済み。既存プロダクトはカード `4px → 3px`、チップ `4px → 2px` の置き換えが主
- [ ] P1（状態くすみ）を最初に入れるプロダクトの選定（候補: DeskBento。枠太さの逸脱を彩度シグナルで一部戻せる可能性）

---

## 更新履歴

- 2026-08-12: 正本を新設（`BRAND_TOKENS.master.md`）。それまで13箇所に個別配布・分岐していたBRAND_TOKENS.mdを、この正本を起点に統一。
- 2026-08-12: v0.1 → v0.2。B1にアンバー（琥珀）`#F2A623`をPatisserieClickerの実測値として追加。B5-3「逸脱記録（プロジェクト別・承認済み）」を新設し、DeskBentoの承認済み逸脱を参照実装例として正式採用。
- 2026-09-18: v0.2 → v0.3。B2① の影を役割別3段スケール（`sm 2px / md 3px / lg 4px`、既定 `md`）に改め、ネスト規則（影の中に影を入れない）を追加。B2④「状態トークン」（hover / active / focus / disabled / CTA文字）を新設。B1 に「CTA の文字色」（いちご＋焦げ茶 3.76:1 の AA 不足に対する 19px ルールと深いちご `#BD4264`）と「くすみ tier」（soft / dusty / deep × 5色相、greige / mocha ニュートラル、使い方 P1〜P3）を新設。テンプレート（Tauri `theme.css` / Flutter `theme.dart`）にアンバーが未反映だったのを修正。
