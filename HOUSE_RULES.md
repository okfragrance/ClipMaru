# AnnyStation HOUSE RULES

**作成日**: 2026-07-12
**根拠**: 6アプリの全体コードレビュー(amitto / Macaron Board / NAGI Workspace / PatisserieClicker / PomoQuest / みどり手帖)の横断分析
**使い方**: 新規プロジェクトの企画時に第2部を読み、`CLAUDE.md` に該当ルールをコピーする。実装中は第3部のチェックリストを使う。

---

# 第1部: 繰り返し起きている問題パターンと根本原因

6アプリのレビュー指摘を並べると、大半が以下の9パターンに収束する。発生数が多い順。

## パターン1: 永続化の「手書き列挙」漏れ 【6アプリ中5で発生・最頻出】

**症状**: UIから変更できるのに再起動で消える状態。ユーザーデータ喪失・購入消滅に直結する。

| アプリ | 実例 |
|---|---|
| Macaron Board | 手帳メモ本文・購入した壁紙・環境音設定など**10件以上**が永続化リスト(APP_KEYS/DATA_KEYS)に未登録 |
| PomoQuest | エリアボスの現在HPが非保存(再起動でフルHP)。streakは保存どころか未実装 |
| amitto | 起動時に保存済み下書きをアクティブ作品で上書き(復元順序の設計ミス) |
| NAGI | 逆方向の失敗: 表示用キャッシュ`_rawTextCache`が保存対象に混入し容量2倍 |
| Macaron Board | スナップショット機能も同じ構造(collectBoardDataの手書き列挙)で分離漏れを再生産 |

**根本原因**: 「状態の追加」と「永続化リストの更新」が**別の場所で手動同期**になっている。状態を1つ足すたびに、保存・復元・(あれば)スナップショット・エクスポートの複数箇所を人間が覚えて更新する構造そのものが欠陥。

## パターン2: 日付・時刻処理の不統一 【4アプリで発生】

**症状**: 「永遠にfalseの比較」「朝9時前だけ壊れる」「日をまたぐと壊れる」。

| アプリ | 実例 |
|---|---|
| Macaron Board | 日付キーが3系統混在(ローカルYYYY-MM-DD / UTC YYYY-MM-DD / MM-DD)。誕生日演出は形式違いで**一度も発動しない**。UTC系はJSTの朝9時前にstreak二重進行 |
| PomoQuest | 保存のたびに`lastDate`を無条件上書き→0時をまたいでプレイすると**日次リセットが恒久スキップ**、デイリー宝箱消滅 |
| NAGI | ヒートマップの「タスク完了」が完了日でなく作成日でカウント |
| Macaron Board | `todayKey()`が2ファイルに重複定義(=不統一の温床) |

**根本原因**: (1) 日付ユーティリティが一元化されておらず、書くたびに`toISOString()`等をアドホックに選んでいる。(2) 「0時をまたいでアプリが動き続ける」シナリオが設計に存在せず、日次リセットが「起動時のみ」実行される。

## パターン3: アプリが見ていない時間の処理(バックグラウンド/スリープ/再起動) 【4アプリで発生】

**症状**: 経過時間の二重計上・計上漏れ・エクスプロイト。

| アプリ | 実例 |
|---|---|
| PomoQuest | バックグラウンド復帰時にタイマー減算とfast-forwardが**二重に**効く / 復帰後の幽霊タイマー |
| PatisserieClicker | オフライン処理が既存トレイの鮮度を劣化させない→**アプリを閉じるのが最適行動**になるエクスプロイト / セーブ時刻の刻み方ミスで放置生産が消失 |
| NAGI | OAuthトークン失効で同期が**黙って**止まる(「設定がリセットされた」ように見える) |
| Macaron Board | blob URLをそのまま保存→再起動で全画像リンク切れ |

**根本原因**: 「時間の経過」を扱うコードが、フォアグラウンドで動き続ける前提で書かれ、pause/resume/kill/オフラインが**後付け**される。「最後に見た時刻(lastSeen)を誰が・いつ刻むか」「復帰時に何を早送りするか」の単一の設計がない。

## パターン4: 報酬・経済のべき等性欠如とバランス未検証 【3アプリで発生】

**症状**: 無限リソース、または長期プレイでゲームが崩壊。

| アプリ | 実例 |
|---|---|
| Macaron Board | タスクのチェック⇔解除往復で**無限コイン**(解除にペナルティなし) / ミニゲーム無制限リプレイで餌無限 / 調合1文字で毎回コイン |
| PatisserieClicker | 転生閾値が固定値のため中盤以降**1周20秒の複利暴走**(50周シミュレーションで判明) |
| PomoQuest | レベル=「現在の」所持アイテム数→消費で**レベルダウン**。全コンテンツ最短9日で消化、以降11ヶ月空白。転生しても周回速度が上がらない |

**根本原因**: (1) 報酬付与が「イベント発生」にだけ紐づき、「このIDには付与済み」という記録がない。付与と取り消しが非対称。(2) 数式を書いた時点では数日分しか頭の中で回しておらず、**数ヶ月分のシミュレーションを実装前後に一度も走らせていない**。

## パターン5: 表示だけで機能していないUI・配線切れ 【6アプリ全部で発生】

**症状**: UIは存在するが押しても・表示されても何も起きない/計算に反映されない。

| アプリ | 実例 |
|---|---|
| PomoQuest | DEF/防具5種が**完全に飾り**(被ダメ機構が存在しない) / 桜テーマの花びらは判定バグで恒久false(1万コインの商品が機能せず) / 図鑑に入手不可能アイテム |
| NAGI | HISTORYプレビューは計算だけして描画されない / 「レイアウト順序」設定はBentoモードでは無効 |
| みどり手帖 | 「天気取得中...」分岐が到達不能なデッドUI |
| Macaron Board | お散歩の「○○を見つけたよ!」は表示だけでアルバム・コインに未反映 / オラクルカードはモデル名ハードコードで設定を無視 |
| PatisserieClicker | (逆の好例: CONFIG全33項目の配線を確認して**ゼロ件**) |

**根本原因**: 「UIを作る」と「ロジックに繋ぐ」が別作業になり、繋いだかどうかを**実際に操作して確認する工程がない**。プロトタイプ移植・機能追加の途中で配線が切れても気づく仕組みがない。

## パターン6: 削除・キャンセル経路の未設計 【3アプリで発生】

**症状**: 正常系(作成・保存)は動くが、やめる・消す・戻すでデータが壊れる。

| アプリ | 実例 |
|---|---|
| amitto | 写真差し替え中にキャンセルすると**元の写真が消える**(キャンセル前に削除を実行済み) / 作品削除で編み図画像が孤児として残る |
| NAGI | クラウド同期に削除の概念(tombstone)がなく、**消したメモが復活する** |
| Macaron Board | 購入済みアイテムを再購入できてコインが消える |

**根本原因**: ハッピーパス偏重。「削除は確定時にまとめて実行」「キャンセルは副作用ゼロ」という原則が決まっていないため、実装者(AI含む)がその場の判断で副作用のタイミングを選んでいる。

## パターン7: コピペ重複 → 片方だけ直る/片方だけバグる 【4アプリで発生】

- Macaron Board: Gemini API呼び出しが3箇所コピペ→**1箇所だけ**モデル名ハードコードでバグ。孵化ロジック2箇所、`todayKey`2箇所
- みどり手帖: `whenText()`があるのに2画面がインラインで重複実装
- PomoQuest: テーマ永続化が2箇所で二重管理

**根本原因**: 「同じ処理を2回目に書いたら共通化する」ルールがない。共通化されていない重複は、修正時に必ず片方が取り残される。

## パターン8: デバッグコード・モック値の本番残留 【3アプリで発生】

- Macaron Board: HabitTrackerの右クリック「テスト用進行」が残留→**streak無限チート**
- みどり手帖: 天気モックが「本物の顔」で表示 / streak初期値4 / `"たった今"`固定文字列
- PomoQuest: `playCoinCheat()`残留(しかも参照アセット未登録)

**根本原因**: モック・デバッグ機能が本番コードと**見た目で区別できない**書き方をされている。リリース前に grep する対象リストもない。

## パターン9: テストがコントローラに届いていない 【構造問題】

- PomoQuest: テストは純粋モデル4ファイルのみ。**重大バグ4件はすべてテスト外のGameController**に居た
- PatisserieClicker: エンジンが純関数+`toSave()/fromSave()`で分離されており、テスト48件全緑・実エンジンをそのままシミュレーションに流用できた(**このリポジトリ群で最も健全な構造**)

**根本原因**: ロジックがUIコントローラ(setState/Timer/ライフサイクルと同居する層)に埋まっていると、テストも書けずシミュレーションもできない。テストの量ではなく**ロジックの置き場所**の問題。

---

# 第2部: 企画・設計段階で最初に決めるルール

新規アプリの企画時に、以下を `CLAUDE.md` / 設計ドキュメントに明記してから実装を始める。

## R1. ストレージ方針を最初に1つ選ぶ(NAGI→amittoルールの一般化)

| プラットフォーム | 標準 | 禁止事項 |
|---|---|---|
| Tauri | `tauri-plugin-sql`(SQLite) | localStorageに実データを置かない |
| PWA/Web | IndexedDB(1レコード=1エンティティ) | localStorageは**小さな設定キーのみ**。実データ・画像・履歴は禁止 |
| Flutter | 実データはローカルDB(drift等)、設定のみSharedPreferences | SharedPreferences一本で全ゲーム状態を持たない |
| 共通 | 画像・バイナリは**blobストアに分離し本文には参照IDのみ** | base64/blob URLを本文・セーブデータに埋め込まない |

- バックアップ/クラウド同期をやるなら、**削除のtombstone(`{id, deletedAt}`)を初日から**スキーマに入れる。後付けは「消したデータが復活する」バグになる(NAGI)。
- エクスポート機能を作るなら、**同じ形式のインポートも同時に作る**(往復できないエクスポートは片手落ち — NAGI)。

## R2. 永続化は「スキーマ駆動」— キー一覧は1箇所だけ

- 永続化対象のキー/フィールドの一覧を**単一の配列・スキーマ定義**に置き、保存・復元・スナップショット・エクスポート・プロフィール切替の**すべてがその1箇所を参照**する。
- 「保存は自動だが復元は手書き」「collectとapplyが別々の列挙」のような**手動同期が2箇所以上ある構造を作らない**。Macaron Boardの永続化漏れ10件超・スナップショット不整合はすべてこれが原因。
- 表示用キャッシュ・一時値は `_` プレフィックスで統一し、保存時にreplacer等で機械的に除外する(NAGIの`_rawTextCache`対策)。

## R3. 日付ユーティリティを最初のコミットで作る

- `utils/date.ts`(等)に `todayKey()` を1つ定義し、**日付キーは常にローカルタイムの `YYYY-MM-DD`**。
- `toISOString().slice(0,10)` は**全面禁止**(JSTでは朝9時前に日付がズレる)。`MM-DD` 形式のキーも禁止(比較不能になる)。
- 日次リセットは**起動時だけでなく「日付が変わった可能性のあるすべての瞬間」**(タイマー完了時・resume時・保存前)にチェックする。「最後に処理した日付」の更新は**ロールオーバー処理を実行した瞬間だけ**(PomoQuestの恒久スキップ対策)。

## R4. 「アプリが見ていない時間」を設計してから実装する

タイマー・ゲーム・同期を含むアプリでは、コーディング前に以下を文書で決める:

1. **時間の出どころは1つ**: `lastSeen` を刻むのは誰か・いつか(セーブのたびに「現在時刻」を刻むのは禁止 — PatisserieClickerの生産消失)。
2. **復帰時の早送り(fast-forward)は1経路**: OSのタイマーが生きているプラットフォーム(Android)では、pause時にタイマーを**止めてから**時刻を記録する(二重計上の防止 — PomoQuest)。
3. **オフライン処理は「新規発生」と「既存状態の経過」の両方**を進める(片方だけだとエクスプロイトになる — PatisserieClickerの鮮度温存)。
4. トークン・接続が切れたら**サイレントに止まらずUIに再接続を出す**(NAGI)。
5. テストシナリオを最初に書く: 「pause→resume」「kill→再起動」「バックグラウンド中に完了→再起動」「0時またぎ」の4つは必須。

## R5. コアロジックはUIから分離する(PatisserieClicker方式を標準に)

- ゲーム/ビジネスロジックは**純関数のエンジン**として書き、`toSave()/fromSave()` でJSONスナップショットを出す。保存先の選択はUI層の責務。
- この構造なら (a)ユニットテストが書ける (b)長期シミュレーションに実エンジンをそのまま流せる (c)ストレージ移行が容易。PomoQuestの重大バグ4件が「テスト外のコントローラ」に集中した教訓。

## R6. 報酬・経済は「付与記録」と「シミュレーション」をセットで

- 報酬は必ず**べき等**に: 「このタスクID/この日/この周回には付与済み」という記録を持ち、チェック⇔解除の往復・リプレイ・再判定で二重取得できない構造にする(Macaron Boardの無限コイン対策)。
- 取り消し操作には対称な巻き戻しを定義する(定義しないなら再付与をブロックする)。
- レベル・進行度は**累計値**から導出する。「現在の所持数」由来にすると消費で逆行する(PomoQuestのレベルダウン)。
- 数式を決めたら、**リリース前に数ヶ月〜1年分のシミュレーションを実際に走らせる**(1日2回/4回/8回など複数パターン)。閾値の固定値は複利成長に必ず追い越される(PatisserieClickerの転生暴走)。エンジンが分離されていれば(R5)スクリプト1本で済む。

## R7. 削除・キャンセルの原則を決めておく

- **破壊的な副作用(ファイル削除・DB削除)は「確定(保存)時」にまとめて実行**。編集中・選択中には実行しない(amittoの写真消失対策。`removedImages`方式)。
- エンティティ削除時は**関連リソース(画像・子レコード・履歴)も同時に消す**チェックリストをスキーマに併記する(孤児ファイル対策)。
- 購入・解禁系は**購入済みチェック**を入れる(重複購入でコイン消失 — Macaron Board)。

## R8. モック・デバッグコードは「見た目で分かる」形でしか書かない

- モックデータは `MOCK_` / `DEBUG_` プレフィックス必須。UIに出すときは開発ビルド限定にするか、明示的な「デモデータ」表示を付ける(みどり手帖の天気モック対策)。
- デバッグ用チート(強制進行・コイン付与)は `kDebugMode` / `import.meta.env.DEV` 等で**コンパイル時にガード**する。「あとで消す」は消えない(Macaron Boardの右クリックチート)。
- リリース前grep対象: `MOCK_` `DEBUG_` `cheat` `test用` `テスト用` `TODO` `console.log`。

## R9. 多重データ(プロフィール/店舗/セーブスロット)は全キー分類表を先に作る

- プロフィール切替・マルチスロット的な機能を作るなら、**全状態キーについて「分離する/共有する」を表で決めてから**実装する。決めずに実装すると必ず中途半端な混在になる(Macaron Board: ペットの進行度は分離なのに覚えた芸は共有、等)。
- この表もR2のスキーマ定義と同じ場所に置く。

---

# 第3部: コーディング時チェックリスト

## 状態・永続化(状態を1つでも追加/変更したら)

- [ ] 新しい状態を永続化スキーマ(R2の一覧)に登録したか
- [ ] **アプリを再起動して値が残ることを実際に確認**したか(最重要・全アプリ共通の穴)
- [ ] スナップショット/プロフィール/エクスポート機能があるなら、そちらにも反映されるか
- [ ] 保存データにblob URL・base64画像・表示用キャッシュが混入していないか
- [ ] `jsonDecode`/`JSON.parse` に try-catch(1キー壊れて起動不能にならないか — PomoQuest)
- [ ] (PWA) sw.js の `CACHE_NAME` をバンプしたか

## 日付・時刻(日付を1箇所でも扱ったら)

- [ ] `todayKey()`(共通ユーティリティ)を使ったか。`toISOString()`で日付キーを作っていないか
- [ ] 0時をまたいで**起動しっぱなし**の場合にどうなるか答えられるか
- [ ] 日次リセットの実行箇所は起動時以外(完了時・resume時)にもあるか
- [ ] 「最後に処理した日付」をリセット処理以外の場所で上書きしていないか

## タイマー・ライフサイクル(時間経過を扱うコードを触ったら)

- [ ] pause→resume で経過時間が二重計上/未計上にならないか(タイマーを止めてから時刻記録)
- [ ] kill→再起動 で状態が正しく復元されるか
- [ ] **バックグラウンド中に完了した状態**で再起動したとき、幽霊タイマーが走らないか
- [ ] オフライン計算は「既存状態の経過」も進めているか(新規発生だけになっていないか)
- [ ] `lastSeen` の刻みどころはセーブ時刻でなく「実際に見ていた最後の時刻」か

## 報酬・経済(報酬付与コードを書いたら)

- [ ] 同じ行為の**往復・連打・リプレイ**で再取得できないか(付与済み記録があるか)
- [ ] 取り消し操作に対称な処理があるか
- [ ] 乱数を使う代入で `random()` を2回呼んで読み書きのキーがズレていないか(PomoQuestのインベントリ破損)
- [ ] 上限・頭打ちのガードがあるか(Lv上限後もコストだけ消費 — Macaron BoardのTerrarium)

## UI・設定(UI要素や設定項目を追加したら)

- [ ] **実際に操作して、効果が出ることを目視確認**したか(表示だけになっていないか)
- [ ] 設定値は本当に参照されているか。ハードコードで上書きしていないか(モデル名等)
- [ ] 条件分岐の判定値は実際に取り得る値か(恒久true/falseになっていないか — 桜エフェクト)
- [ ] 計算したのに描画していない値はないか(NAGIのプレビュー)

## 削除・キャンセル(削除/編集モーダルを書いたら)

- [ ] キャンセルしたとき副作用ゼロか(削除の実行は確定時か)
- [ ] エンティティ削除で関連リソース(画像・子レコード)も消えるか
- [ ] 同期機能があるなら削除が他端末に伝播するか(tombstone)
- [ ] 購入・解禁は重複実行をブロックしているか

## コード品質(常時)

- [ ] 同じ処理を**2回目に書いた瞬間に**共通化したか(コピペ3箇所目は禁止)
- [ ] デバッグ/チートコードはビルドフラグでガードされているか
- [ ] モック値に `MOCK_` プレフィックスが付いているか

## リリース前(毎回)

- [ ] `MOCK_` `DEBUG_` `cheat` `テスト用` `TODO` `console.log` をgrep
- [ ] 再起動テスト: 全機能の状態が残るか一巡
- [ ] 0時またぎテスト(端末時刻を23:58にして完了させる)
- [ ] バックグラウンド10分→復帰テスト
- [ ] ゲームなら経済シミュレーション(複数プレイパターン×数ヶ月分)を再実行
- [ ] エクスポートしたデータがインポートで往復できるか
- [ ] 未使用ファイル・開発スクリプトが配布物に混入していないか(NAGIの`nagi_public/`内Nodeスクリプト)

---

# 付録: 各アプリの「今すぐ直すべき」対応状況メモ

(直したらチェックを入れる)

- [ ] **Macaron Board**: 永続化漏れ(plannerMemos他) / タスクトグル無限コイン / 右クリックチート / 日付3系統統一 / オラクルカードのモデルハードコード
- [ ] **NAGI**: `_rawTextCache`除外 / 削除tombstone / base64画像分離 / 検索ハイライト正規表現 / 手動テーマ上書き
- [ ] **PatisserieClicker**: 鮮度オフライン温存 / `toSave()`のlastSeen / 転生閾値スケール化
- [ ] **PomoQuest**: ボス報酬インベントリ破損(C-1) / BG二重カウント(T-1) / 日次リセット恒久スキップ(T-2) / 幽霊タイマー(T-3) / Android14通知フォールバック(T-4)
- [ ] **みどり手帖**: ホームverticalScroll / ナビ折り返し / Wx/WX削除 / シェアのパン計算統一
- [ ] **amitto**: 下書き上書き / 写真キャンセル消失 / できたに移すでフォーム破棄 / 孤児chartImage
